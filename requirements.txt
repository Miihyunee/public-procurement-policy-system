# HTTP 클라이언트 — 정부 Open API / 웹 데이터 수집
httpx>=0.27,<1.0

# Excel 파싱 — 기관 구매실적 업로드 파일 처리
openpyxl>=3.1,<4.0

# 데이터 처리 — 구매실적 매칭 및 집계
pandas>=2.2,<3.0

# 데이터 검증 및 직렬화 — API 요청/응답, 내부 모델
pydantic>=2.7,<3.0