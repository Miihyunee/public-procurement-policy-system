# 담당자 검토 인터페이스 설계

## 문서 정보

| 항목 | 값 |
|---|---|
| 작성일 | 2026-08-22 |
| 상태 | ✅ **구현 완료** (STEP 3 최초 · STEP 6 개선). ⛔ 분석기 연결은 여전히 없음 |
| 최종 갱신 | 2026-08-22 (STEP 6) |
| 상위 문서 | `NEW_ARCHITECTURE.md` · `DATABASE_PIPELINE_DESIGN.md` |
| ⛔ 아직 아닌 것 | 분석 방법 선택 · 자동 확정 기준 · 그룹 뷰 · Electron/Rust 선택 · 상계 로직 수정 |

> **핵심 원칙**: 담당자는 **원본을 보고, 후보를 참고해, 스스로 확정**합니다.
> 시스템은 원본을 절대 덮어쓰지 않고, 무엇이 원본이고 무엇이 판단인지 화면에서
> 구분해 보여줍니다.

---

# 1. 화면 구성

## 1.1 위치

기존 `web/static/index.html` (1,157줄) 은 카드형 단일 페이지입니다. 여기에
**"검토" 영역을 추가**하고 기존 영역은 건드리지 않습니다.

```text
┌─ 현황 ────────┐  ┌─ 정책 카드 ──┐  ┌─ 달성률 ──┐   ← 기존 (유지)
└───────────────┘  └──────────────┘  └───────────┘
┌─ 업로드 ──────┐                                    ← 기존 (유지)
└───────────────┘
┌─ 구매유형 검토 ───────────────────────────────┐    ← 🆕 신규
└───────────────────────────────────────────────┘
```

## 1.2 검토 목록

```text
구매유형 검토                              확정 1,203 / 2,292 (52.5%)
─────────────────────────────────────────────────────────────────
필터: [전체 ▾] [⚠️ 확인 필요] [미확정] [확정됨]      정렬: [⚠️ 우선 ▾]

┌───────────────────────────────────────────────────────────────┐
│ 적요        시설물 유지관리                                    │
│ 거래처      (주)한빛산업개발      사업자번호  220-81-62517      │
│ 금액        54,648,000원          결의일자    2026-03-15        │
│ 예산과목    외주용역비                                          │
│ ─────────────────────────────────────────────────────────────  │
│ ⚠️ 확인 필요 — 후보가 갈립니다                                  │
│    추천 1  용역  72%   근거: 유사 확정 사례 3건                 │
│    추천 2  공사  68%   근거: '유지관리' 키워드                  │
│ ─────────────────────────────────────────────────────────────  │
│ 담당자 선택   [ 공사 ]  [ 용역 ]  [ 물품 ]  [ 판단 보류 ]       │
│ 메모          [                                        ]        │
│                                            [ 확정 ]             │
└───────────────────────────────────────────────────────────────┘
```

명확한 건:

```text
│ 적요        LED 교체공사                                        │
│ ✅ 후보                                                         │
│    추천 1  공사  97%   근거: '교체공사' 완전 일치               │
│    추천 2  용역  21%                                            │
│ 담당자 선택   [ 공사 ]  [ 용역 ]  [ 물품 ]  [ 판단 보류 ]       │
```

> ⚠️ **97% 여도 자동 선택되어 있지 않습니다.** 버튼은 전부 비선택 상태로
> 시작합니다. 미리 골라 두면 담당자가 그대로 눌러 **사실상 자동 확정**이
> 됩니다. 고객이 확정한 분류 규칙은 예산과목 3건뿐입니다.

## 1.3 확정 후 표시

```text
│ 적요        시설물 유지관리          [원본]                     │
│ ─────────────────────────────────────────────────────────────  │
│ AI 분석     용역 72% · 공사 68%      [분석]                     │
│ 담당자 결정 공사                      [확정]                     │
│ 확정일      2026-08-22 14:31         확정자  김담당              │
│ 메모        현장 시공 포함 건                                    │
│                              [ 이력 보기 ]  [ 다시 검토 ]        │
```

**세 값이 화면에서 물리적으로 분리**되어 있습니다 — 원본 / AI / 담당자.
원본 적요를 `"시설물 유지관리" → "공사"` 로 덮어쓰는 표시는 **하지 않습니다.**

## 1.4 최종 확정

```text
┌─ 최종 확정 ────────────────────────────────────────────────┐
│ 검토 완료 1,203 / 2,292    미확정 1,089                     │
│ ⚠️ 마지막 확정(2026-08-20) 이후 42건이 변경되었습니다        │
│                          [ 최종 확정 — DB-3 생성 ]          │
└─────────────────────────────────────────────────────────────┘
```

이 버튼이 DB-3 을 만듭니다(`DATABASE_PIPELINE_DESIGN.md` §5). 미확정 행의
처리는 🔴 결정 대기입니다.

---

# 2. 담당자가 보는 정보 (지시 8번)

화면은 **네 블록**으로 나뉩니다. 블록이 섞이면 "원본 적요가 공사로 바뀌었다"
같은 오해가 생기므로, 응답 스키마 수준에서 분리해 둡니다.

## 2.1 원본 (DB-1 · ⛔ 전부 읽기 전용)

| 화면 라벨 | 출처 |
|---|---|
| 적요 | `purchase.description` |
| 신고기준일 | `purchase.issue_date` |
| 결의일자 | `purchase.resolution_date` |
| 거래처명 | `purchase.company_name` |
| 사업자번호 | `purchase.business_no` |
| 금액(VAT 포함) | `purchase.amount` |
| 예산과목 | `purchase.budget_account` |

> ⚠️ **공급가액은 DB-1 에 별도로 보관되지 않습니다.** 원본 엑셀에는 공급가액 ·
> 세액 · 계 세 컬럼이 있으나, 표준 양식은 **VAT 포함 총액 한 값**만 받습니다
> (`DECISIONS.md` 금액 기준). 공급가액을 화면에 따로 보여주려면 스키마 변경이
> 필요하며, 이는 **결정 대기 항목**입니다.

## 2.2 분석 결과 (DB-2 · ⛔ 읽기 전용 · **확정이 아님**)

| 화면 라벨 | 출처 |
|---|---|
| 분석 방법 | `analyzer_name` + `analyzer_version` |
| 후보 유형 · 후보 순위 | `candidates_json[].purchase_type` (점수 내림차순) |
| 후보 점수 | `candidates_json[].score` |
| 후보 개수 | 후보 목록에서 계산 |
| 1·2순위 점수차 | 후보 1·2번 점수의 차. 후보가 1개 이하면 **`null`** |
| 근거 | `candidates_json[].evidence` |
| 확인 권장 표시 | `is_ambiguous` |

> ⛔ **점수차에 기준이 없습니다.** "차이가 크면 확정" 같은 임계값을 만들지
> 않았습니다(고객 미확정). 숫자를 그대로 보여줄 뿐입니다.

## 2.3 같은 적요의 과거 확정 이력 (DB-1 + DB-2 · ⛔ 추천이 아님)

| 화면 라벨 | 뜻 |
|---|---|
| 과거 N건 + 유형별 건수 | 같은 적요를 과거에 어떻게 확정했는지 |
| **일관성** | `이력 없음` / `한 유형으로만 확정됨` / `여러 유형으로 갈림` |
| **최다 유형 + 비율** | 가장 많이 고른 유형과 그 비율(%) |
| **유형 수** | 과거에 붙은 서로 다른 유형의 개수 |
| "과거 판단이 갈렸던 적요입니다" | 같은 적요가 **여러 유형**으로 확정된 적 있음 |
| "1순위 후보가 과거 최다 유형과 다릅니다" | 분석과 과거 판단이 갈림 |
| **"과거 확정 이력 없음 (처음 보는 적요)"** | 이력이 없음. ⛔ **오류가 아니며** 임의 유형을 붙이지 않음 |

> ⛔ **일관성은 등급이 아닙니다.** "높음/보통/낮음" 처럼 비율을 잘라 만든 값이
> 아니라, **"이력이 있는가 · 갈렸는가" 라는 구조적 사실** 세 가지뿐입니다.
> 갈린 **정도**는 최다 유형 비율로 원시값 그대로 보여주며, 그 값을 어디서
> 자를지는 🔴 고객 확인 사항입니다.

> **왜 별도 블록인가**: `review` 블록 안에 넣으면 "확정값" 으로 오인됩니다.
> 과거 기록을 **세어서 보여줄 뿐**이며, 어떤 유형이 맞다고 말하지 않습니다.
>
> **판단 보류(`None`)는 이력에 세지 않습니다** — 사람이 결론을 내지 않은
> 것이기 때문입니다.

## 2.4 담당자 결정 (DB-2 · ✅ **여기서만 편집**)

| 선택지 | 값 |
|---|---|
| 공사 | `CONSTRUCTION` |
| 용역 | `SERVICE` |
| 물품 | `GOODS` |
| **판단 보류** | `null` — 새 유형이 아니라 **값 없음** |
| 메모 | `review_note` |

**화면 전체에서 편집 가능한 것은 유형 선택과 메모 두 개뿐입니다.**

> ⛔ **어떤 선택지도 미리 눌려 있지 않습니다.** 분석 1순위가 100% 여도
> 마찬가지이며, 화면에 "선택된 항목이 없습니다" 라고 명시합니다. 미리 눌러
> 두면 담당자가 그대로 확정을 눌러 **사실상 자동 확정**이 됩니다.

---

# 3. 담당자가 할 수 있는 동작

| 동작 | 결과 |
|---|---|
| 유형 선택 | `final_purchase_type` 설정 (아직 미확정) |
| **확정** | `review_status = CONFIRMED` · `reviewed_by/at` 기록 · 이력 추가 |
| **판단 보류** | `final_purchase_type = NULL` · `review_status = PENDING` 유지 |
| 다시 검토 | `review_status = REOPENED` · 이전 값은 이력에 남음 |
| 메모 | `review_note` |
| 이력 보기 | `purchase_review_history` 조회 |

⛔ **원본 수정 · 행 삭제 동작은 제공하지 않습니다.**

**"판단 보류"** 를 넣은 이유: 담당자가 정말 모를 때 아무 값이나 고르게
만들면 안 됩니다. 미확정으로 남기는 것이 정직한 상태입니다.

---

# 4. API 설계 (개념 · 미구현)

기존 FastAPI 패턴(`app.py`)을 그대로 따릅니다. UI 프레임워크에 종속되지 않도록
**모든 동작은 HTTP API** 로만 제공합니다.

| 메서드 | 경로 | 용도 |
|---|---|---|
| `GET` | `/reviews` | 검토 목록 (필터·정렬·페이징) |
| `GET` | `/reviews/{purchase_id}` | 한 건 상세 (원본 + 후보 + 확정) |
| `PUT` | `/reviews/{purchase_id}` | 유형 확정 · 메모 |
| `POST` | `/reviews/{purchase_id}/reopen` | 재검토 |
| `GET` | `/reviews/{purchase_id}/history` | 변경 이력 |
| `GET` | `/reviews/summary` | 진행률 (확정/미확정/확인 필요) |
| `POST` | `/final-datasets` | **최종 확정** — DB-3 생성 |
| `GET` | `/final-datasets` | 생성 이력 (ACTIVE / SUPERSEDED) |

## 4.1 응답 예 (`GET /reviews/{purchase_id}`)

```json
{
  "purchase_id": 1283,
  "source": {
    "description": "시설물 유지관리",
    "company_name": "(주)한빛산업개발",
    "business_no": "2208162517",
    "amount": "54648000",
    "resolution_date": "2026-03-15",
    "issue_date": "2026-03-10",
    "budget_account": "외주용역비"
  },
  "analysis": {
    "status": "ANALYZED",
    "analyzer_name": "…", "analyzer_version": "…",
    "is_ambiguous": true,
    "candidates": [
      {"purchase_type": "SERVICE", "label": "용역", "score": "0.72", "evidence": "유사 확정 사례 3건"},
      {"purchase_type": "CONSTRUCTION", "label": "공사", "score": "0.68", "evidence": "'유지관리' 키워드"}
    ]
  },
  "review": {
    "status": "PENDING",
    "final_purchase_type": null,
    "reviewed_by": null, "reviewed_at": null, "review_note": null
  }
}
```

> **`source` · `analysis` · `review` 를 응답에서부터 분리**했습니다. 화면이
> 셋을 섞을 수 없게 만드는 것이 목적입니다.

## 4.2 쓰기 API 의 안전장치

| 규칙 | 방법 |
|---|---|
| 원본을 못 쓴다 | `ReviewApiService` 에 `PurchaseRepository` **쓰기 메서드를 주입하지 않음** |
| 허용값만 받는다 | `final_purchase_type` ∈ `PURCHASE_TYPES ∪ {null}` |
| 이력이 남는다 | 확정·재검토 시 항상 `purchase_review_history` 추가 |
| 인증 | 기존 `admin/auth.py::require_admin_token` 패턴 재사용 검토 |

---

# 5. 상계 검토와의 통합 (지시 10번)

## 5.1 현재 상태

`core/offsetting.py` 는 이미 **담당자 확인 대상**을 만들어 냅니다.

```python
@dataclass(frozen=True, kw_only=True)
class NeedsManualReviewGroup:
    key: MatchKey                       # 기업 · 사업자번호 · 금액
    negative: Purchase
    candidates: list[Purchase]          # 후보 전부
    reason: ReviewReason                # MULTIPLE_CANDIDATES | CONTESTED_CANDIDATE
    sibling_negatives: list[Purchase]
    def candidate_lines(self) -> tuple[str, ...]: ...
```

실측(2026년 2,292행): 자동 상계 42쌍 · **담당자 확인 84건** · 짝 없음 3건.

## 5.2 구조가 이미 같습니다

| | 구매유형 검토 | 상계 검토 |
|---|---|---|
| 후보 제시 | `ClassificationResult.candidates` | `NeedsManualReviewGroup.candidates` |
| 자동 확정 | ⛔ 안 함 | ⛔ 안 함 |
| 담당자 선택 | 유형 3택 | 후보 양수 중 1택 |
| 결과 저장 | DB-2 `final_purchase_type` | DB-2 `offset_review.chosen_purchase_id` |

**같은 검토 화면에 탭으로 넣을 수 있습니다.**

```text
구매유형 검토          상계 검토 (84건)
─────────────────────────────────────────
┌───────────────────────────────────────────────────────────┐
│ (−) 16,900원  엘지전자(주)  107-86-14075  발행 2026-01-05  │
│ 어느 (+) 거래와 상계할까요?                                 │
│  ○ 발행 2026-01-05 | 적요 01/05 물품대 외 | 예산과목 (공란) │
│  ○ 발행 2026-01-05 | 적요 01/05 물품대 외 | 예산과목 (공란) │
│  ○ 발행 2026-01-05 | 적요 수도권…정산 건 | 예산과목 각종수수료│
│  ○ 상계하지 않음                                           │
│  ℹ️ G20 지출결의서(세금계산서·비고란)를 확인해 판단하세요.  │
│                                              [ 확정 ]      │
└───────────────────────────────────────────────────────────┘
```

## 5.3 ⛔ 이번에 하지 않는 것

| 금지 | 상태 |
|---|---|
| `core/offsetting.py` 수정 | ⛔ 하지 않음 |
| `offset_review` 테이블 생성 | ⛔ 하지 않음 |
| 상계 확정 API·화면 구현 | ⛔ 하지 않음 |
| G20 로그인·조회·자동 수집 | ⛔ **영구 범위 밖** |

담당자가 G20 을 **직접** 확인하고, 시스템은 그 결과만 받습니다.
🔴 결정 ③·④ 확정 후 별도 승인 대상입니다.

---

# 6. UI 프레임워크 독립성

| 계층 | 내용 | 교체 가능 |
|---|---|---|
| 화면 | HTML/JS · Electron · Rust · CLI | ✅ |
| API | `/reviews` · `/final-datasets` (JSON) | 경계 |
| 서비스 | `ReviewApiService` → `ReviewService` → Repository | ⛔ 고정 |

⛔ **JavaScript 에 업무 판정을 두지 않습니다.** 후보 정렬·이중 매칭 판정·유형
허용값은 전부 백엔드가 소유합니다. 화면은 받아서 그립니다. 이 원칙은 이미
`test_electron_upload_wiring.py` 가 업로드 화면에 대해 강제하고 있으며, 검토
화면도 같은 테스트를 붙일 수 있습니다.

---

# 7. 🔴 결정 대기

| # | 항목 | 상태 |
|---|---|---|
| ① | 미확정 행을 DB-3 에 포함할 것인가 | 🔴 `DATABASE_PIPELINE_DESIGN.md` §5.3 |
| ② | 검토 화면에 인증(관리자 토큰)이 필요한가 | 🔴 미확정 |
| ③ | `reviewed_by` 를 어떻게 얻는가 (로그인 없음) | 🔴 미확정 — 직접 입력? 설정값? |
| ④ | 상계 검토를 같은 화면에 넣을 것인가 | 🔴 결정 ③·④ 후 |
| ⑤ | 2,292건을 전부 검토해야 하는가, 일부만인가 | 🔴 미확정 — 운영 부담 |
| ⑥ | 일괄 확정(같은 적요 묶어서) 허용 여부 | 🔴 미확정 — 편의 vs 정확도 |

> ⑤ 는 현실적으로 중요합니다. 2,292건을 한 건씩 보는 것은 부담이 큽니다.
> "확인 필요 건만 검토하고 나머지는 후보 1순위로 일괄" 같은 방식은 **사실상
> 자동 확정**이므로 ⛔ 임의로 넣지 않았습니다.

---

# 8. 테스트 계획 (설계만)

| # | 항목 | 기대 |
|---|---|---|
| 1 | 목록 조회 | 원본 + 후보 + 확정 상태가 **분리되어** 반환 |
| 2 | 후보 선택 | `final_purchase_type` 설정 |
| 3 | 확정 | `CONFIRMED` · `reviewed_by/at` 기록 |
| 4 | 판단 보류 | `final_purchase_type = NULL` · `PENDING` 유지 |
| 5 | 수정 | 이전 값이 이력에 남음 |
| 6 | 재검토 | `REOPENED` 후 재확정 가능 |
| 7 | 이력 조회 | 시간순 · 이전/이후 값 포함 |
| 8 | ⛔ 원본 미수정 | 검토 API 호출 후 `purchase` 행 동일 |
| 9 | ⛔ 허용값 강제 | 3값 + `null` 외 요청은 422 |
| 10 | ⛔ 자동 선택 없음 | 응답의 `final_purchase_type` 이 초기에 `null` |
| 11 | 재분석 | 확정값이 덮이지 않음 |
| 12 | 진행률 | 확정/미확정/확인 필요 건수 정확 |
| 13 | ⛔ 화면에 업무 로직 없음 | 검토 JS 에 유형 판정·임계값 부재 (AST/문자열 검사) |
| 14 | 최종 확정 | 새 dataset 생성 · 이전 SUPERSEDED |
| 15 | 확정 후 변경 감지 | "N건 변경됨" 경고가 정확 |

---

# 9. 목록 다루기 — 검색 · 필터 · 정렬 · 페이지 (STEP 8)

> **구분 표기** ✅ 확정된 기술사항 · 🔴 고객 확인 필요 · ⛔ 하지 않은 것

## 9.1 ✅ 확정된 기술사항

| 기능 | 값 | 비고 |
|---|---|---|
| **적요 검색** | 부분 문자열 · 대소문자·띄어쓰기 무시 | 빈 값이면 전체 |
| **상태 필터** | `ALL` · `PENDING` · `CONFIRMED` · `REOPENED` | ⛔ 새 상태를 만들지 않음 |
| **확정 여부** | `ALL` · `DECIDED` · `UNDECIDED` | 재검토(`REOPENED`)는 **미확정** |
| **과거 이력** | `ALL` · 이력 있음 · 이력 없음 · 혼재 · 1순위=과거최다 · 1순위≠과거최다 | |
| **후보 수** | `ALL` · 0개 · 1개 · 2개 이상 | `candidate_count` 그대로 |
| **정렬 기준** | 구매ID · 결의일자 · 신고기준일 · 금액 · 적요 · 상태 · 후보 수 · 점수차 · 과거 확정 건수 · 과거 최다 비율 | 담당자가 고름 |
| **정렬 방향** | 오름차순 · 내림차순 | |
| **페이지 크기** | 기본 20 · 최대 200 | 화면 성능용 값 |

**정렬에서 "값 없음" 은 방향과 무관하게 늘 뒤로** 갑니다. 후보가 1개면 점수차라는
것이 **존재하지 않으므로**, 내림차순으로 바꿨다고 빈 값이 앞으로 몰려오면
담당자가 빈 칸부터 보게 됩니다.

> ⛔ **`score_gap` · `dominant_ratio` 로 줄을 세울 수는 있지만, 그것이 "먼저 봐야
> 할 순서" 라는 뜻은 아닙니다.** 위험도·우선순위·자동검토 대상 같은 개념을
> 만들지 않았습니다.

## 9.2 ✅ 처리 위치 — 서버

```text
담당자가 고른 조건
      ↓  쿼리 문자열
GET /reviews?search=…&status=…&sort=…&page=…
      ↓
ReviewService.search()   ← 거르고 · 줄 세우고 · 잘라낸다
      ↓
해당 페이지만 응답        ← 전체를 브라우저로 보내지 않는다
```

**왜 브라우저에서 거르지 않는가**: 2,292건이면 견딜 수 있어도, 몇 만 건이 되면
첫 화면에서 전부 내려받느라 느려지고 메모리도 그만큼 씁니다. 화면이 받는 양을
**페이지 크기로 고정**해 두면 건수가 늘어도 첫 화면 비용이 일정합니다.

**아직 SQL 로 내리지 않은 이유**: `score_gap` 과 과거 이력 관련 조건은
**컬럼이 아니라 계산값**입니다. `score_gap` 은 후보 JSON 을 풀어야 나오고, 과거
이력은 DB-1(적요)과 DB-2(확정)를 묶어야 나옵니다. 지금은 서비스 계층에서
계산하며, 다음 항목이 필요해지면 SQL 로 내립니다.

| 조건 | 현재 | SQL 로 내릴 수 있는가 |
|---|---|---|
| 적요 검색 · 상태 · 확정 여부 | 서비스 | 🟢 가능(정규화 컬럼 추가 시) |
| 후보 수 · 점수차 | 서비스 | 🟡 후보를 컬럼으로 펴야 함 |
| 과거 이력 조건 | 서비스 | 🟡 DB-1 · DB-2 조인 + 집계 필요 |

> 🟡 **성능 개선은 필요해질 때 합니다.** 지금 데이터로는 체감되지 않으며,
> 미리 최적화하면 조인 구조를 고객 답변 전에 굳히게 됩니다.

## 9.3 ✅ 과거 이력 색인의 수명 (PastLabelIndex)

**바뀐 것**: 예전에는 한 건을 조회할 때마다 색인을 처음부터 다시 만들었습니다
(구매 전체 + 검토 전체를 매번 읽음).

```text
[이전]  get_target() → 색인 새로 생성 (전체 2회 스캔) → 조회
[현재]  get_target() → 지문 조회(집계 1회) → 같으면 재사용 / 다르면 재생성
```

**낡은 값을 주지 않는 방법**: 서비스가 기억하는 상태가 아니라 **DB 의 지문**으로
판단합니다.

```sql
SELECT COUNT(*), MAX(reviewed_at) FROM purchase_review WHERE review_status = 'CONFIRMED'
```

| 상황 | 지문 변화 | 결과 |
|---|---|---|
| 새 확정 | 건수 ↑ | 재생성 |
| `CONFIRMED` → `REOPENED` | 건수 ↓ | 재생성 |
| 같은 건을 다른 유형으로 재확정 | 확정 시각 변경 | 재생성 |
| 판단 보류로 변경 | 확정 시각 변경 | 재생성 |
| **Repository 를 직접 고침**(테스트 등) | 위와 동일 | 재생성 |
| 재분석 | **변화 없음** | 재사용 |

`updated_at` 이 아니라 `reviewed_at` 을 보는 이유: `updated_at` 은 **재분석에도**
바뀌는데, 분석은 확정 이력을 건드리지 않으므로 그걸로 판단하면 아무 이유 없이
색인을 다시 만들게 됩니다.

## 9.4 ✅ 확정 이력 CSV 내보내기

`GET /reviews/export.csv` — **화면과 같은 조건**(페이지 조건만 제외)으로 전부.

| 항목 | 값 |
|---|---|
| 인코딩 | UTF-8 **BOM** — 엑셀에서 한글이 깨지지 않도록 |
| 줄바꿈 | CRLF |
| 열 순서 | **고정** (담당자 엑셀 수식이 깨지지 않도록) |
| 빈 값 | 빈 칸으로 통일 (`0` 과 구분) |
| 날짜 | `YYYY-MM-DD` · 일시는 `YYYY-MM-DD HH:MM:SS` |
| 금액 | 지수 표기 없이 숫자 그대로 |
| 안전성 | `=` `+` `-` `@` 로 시작하는 값 앞에 `'` — 엑셀 수식 실행 방지 |
| 메모리 | 한 줄씩 흘려보냄(제너레이터) — 건수가 늘어도 일정 |

**20개 열**: 구매ID · 신고기준일 · 결의일자 · 적요 · 거래처명 · 사업자번호 ·
금액 · 예산과목 · 현재 상태 · **최종 유형** · 확정자 · 확정일시 · 검토 메모 ·
분석 방법 · 분석 1순위 · 과거 확정 건수 · 과거 확정 최다 유형 · 과거 최다 유형
비율 · 과거 확정 유형 수 · 과거 이력 일관성

> ⛔ **`최종 유형` 열에는 담당자 확정값만 들어갑니다.** 과거 최다 유형이나 분석
> 1순위를 대신 넣지 않습니다 — 참고 정보가 확정값으로 둔갑하면, 담당자가 정하지
> 않은 값이 엑셀에서 확정처럼 읽힙니다. 이를 테스트가 막습니다
> (`test_review_index_and_export.py::test_history_never_becomes_the_decision`).

## 9.5 🔴 아직 미정 (이번에도 정하지 않음)

| 항목 | 상태 |
|---|---|
| 자동 확정 | 🔴 없음 |
| `score` · `score_gap` 임계값 | 🔴 없음 |
| `dominant_ratio` 임계값 | 🔴 없음 |
| `needs_review` 판정 | 🔴 없음 |
| 그룹 일괄 확정 · 그룹핑 키 | 🔴 없음 (`DESCRIPTION_GROUPING_ANALYSIS.md`) |
| 분석 방법(BM25/RAG/FUSE) 선택 | 🔴 없음 — 운영 기본은 `NoRuleClassifier` |
| 구매유형 업무규칙 | 🔴 고객 확인 (Q5-1~Q5-7) |

---

# 10. 연속 검토 업무 흐름 (STEP 9)

> **구분 표기** ✅ 현재 구현 · 🔴 아직 미정

## 10.1 ✅ 현재 구현

| 기능 | 상태 | 비고 |
|---|---|---|
| 검색 · 필터 · 정렬 · 페이지 | ✅ | §9 |
| **변경 이력 조회** | ✅ | 카드마다 "이력 보기" → 모달 |
| **연속 검토** | ✅ | 확정하면 같은 자리에 다음 건이 들어옴 |
| **이전/다음 대상 이동** | ✅ | 버튼 + `N` / `P` 키 |
| **전체 진행률** | ✅ | 필터와 무관 |
| **현재 조건 진행률** | ✅ | 조건 안에서의 확정 비율 |
| **URL 상태 유지** | ✅ | 새로고침 · 뒤로가기 · 공유 |
| CSV 내보내기 | ✅ | §9.4 |

## 10.2 ✅ 변경 이력 — 지금 값과 과거를 분리

모달은 **두 블록**으로 나뉩니다.

```text
┌ 지금 값 (현재 상태 기준) ─────────────┐
│ 상태 CONFIRMED · 확정 유형 물품 · 확정자 김담당 │
└──────────────────────────────────┘
┌ 변경 이력 (과거 기록 · 시간순) ────────┐
│ …  ANALYZED  bm25    -    →  -        │
│ …  CONFIRMED 김담당   -    →  공사      │
│ …  REOPENED  김담당   공사  →  공사      │
│ …  CONFIRMED 김담당   공사  →  물품      │
└──────────────────────────────────┘
```

- **지금 값은 `review` 블록에서** 가져옵니다. ⛔ 이력의 아무 줄을 현재값으로
  쓰지 않습니다 — 위 예에서 첫 확정은 "공사" 지만 **지금은 "물품"** 입니다.
- 미확정인데 이전 유형이 남아 있는 경우(`REOPENED`)에는 **`(미확정)`** 으로
  표시합니다.
- 유형 코드는 백엔드가 준 선택지 목록으로 **한글 라벨**로 바꿔 보여줍니다.
- 이력을 열 때 **그 한 건의 이력만** 부릅니다(목록을 다시 받지 않음).

## 10.3 ✅ 연속 검토 — 이동만, 판단은 사람이

```text
유형 선택 → 확정 → 목록 갱신 → 같은 자리에 다음 건
```

| 상황 | 동작 |
|---|---|
| 확정한 건이 현재 필터에서 빠짐 | 같은 자리에 **다음 건**이 들어옴 |
| 커서가 페이지 끝을 넘음 | 다음 쪽을 불러오고 **첫 건**으로 |
| 커서가 페이지 앞을 넘음 | 이전 쪽을 불러오고 **마지막 건**으로 |
| 마지막 대상 | "마지막 검토 대상입니다" 안내. **오류 아님** |
| 조건에 맞는 건이 0 | "현재 조건에 맞는 검토 대상이 없습니다" |

이동 중에도 **검색·필터·정렬·페이지 크기가 그대로 유지**됩니다.
⛔ 새 우선순위를 계산하지 않고, **담당자가 고른 정렬 순서 그대로** 다음 칸입니다.

> ⛔ **다음 건의 유형을 미리 고르거나 확정하지 않습니다.** 이동만 합니다.
> `moveCursor` 안에 `confirmReview` 나 `reviewPicks` 대입이 없다는 것을
> 테스트가 막습니다.

## 10.4 ✅ 키보드 — 이동만 배정

| 키 | 동작 |
|---|---|
| `N` | 다음 검토 대상 |
| `P` | 이전 검토 대상 |
| `Esc` | 이력 모달 닫기 |

**충돌 방지**

| 상황 | 처리 |
|---|---|
| 입력창 · 메모창 · 선택 상자에 포커스 | 단축키 **동작 안 함** (`isTyping`) |
| `contenteditable` 요소 | 〃 |
| 이력 모달이 열려 있음 | 뒤쪽 목록이 움직이지 않음 |
| `Ctrl` · `Cmd` · `Alt` 조합 | 브라우저·OS 몫으로 넘김 |

> ⛔ **확정 단축키를 만들지 않았습니다.** `Enter` 한 번에 확정되면 담당자가
> 보지도 않은 건이 확정될 수 있습니다. 확정은 **반드시 버튼**입니다. 어떤
> 키도 `confirmReview` 로 이어지지 않는다는 것을 테스트가 막습니다.

## 10.5 ✅ 진행률 — 두 가지를 나란히

```text
전체 확정 17 / 46 · 확인 권장 23        ← 필터와 무관
현재 조건 확정 0 / 29 (0.00%)          ← 지금 걸어 둔 조건 안에서
```

**조건이 전체와 같으면** 같은 숫자가 두 번 나오므로 두 번째 줄을
`현재 조건 = 전체` 로 대신합니다. 숫자를 중복해 보여주면 어느 쪽을 봐야
하는지 오히려 헷갈리기 때문입니다.

⚠️ **미확정 필터에서는 조건 진행률이 항상 0% 입니다.** 확정하는 순간 그 건이
필터에서 빠지기 때문이며, 정상입니다. 이때 의미 있는 값은 **분모(남은 건수)**
이고, 실제로 확정할 때마다 줄어듭니다(29 → 28 → 27 …).

> ⛔ **평가 기준이 아닙니다.** "50% 미만이면 위험" 같은 판정을 붙이지 않으며,
> `ConditionProgress` 에 그런 필드를 만들 수 없도록 테스트가 막습니다.

## 10.6 ✅ URL 상태

```text
/?search=청소&status=ALL&decision=UNDECIDED&history=ALL&candidates=ALL
 &sort=amount&direction=desc&page=2&page_size=10
```

파라미터 이름은 **API 정의를 그대로** 씁니다(화면 전용 이름을 따로 만들지 않음).

| 상황 | 동작 |
|---|---|
| 새로고침 | 조건·쪽 그대로 복원 |
| 조건 변경 | `pushState` — 뒤로가기로 되돌아감 |
| 첫 로딩 · 커서 이동 | `replaceState` — history 를 불리지 않음 |
| 뒤로/앞으로 | `popstate` 에서 URL 을 정본으로 복원 |
| URL 공유 | 같은 목록이 재현됨 |
| 허용되지 않는 값 | 화면이 무시하고 기본값 유지 (서버도 422 로 거부) |
| `page=0` 등 이상값 | 1쪽으로 |

## 10.7 ✅ 요청 수 (실측)

| 동작 | 목록 요청 | 이력 요청 |
|---|---|---|
| 필터 변경 · 정렬 변경 · 페이지 이동 | 1 | 0 |
| 검색어 두 글자 타이핑 | **1** (debounce 300ms) | 0 |
| `N` · `P` 이동 (같은 쪽 안) | **0** | 0 |
| 이력 모달 열기 | **0** | 1 |
| 모달 닫기 | 0 | 0 |
| 확정 | 1 | 0 |

## 10.8 🔴 아직 미정 (이번에도 정하지 않음)

| 항목 | 상태 |
|---|---|
| 자동 확정 | 🔴 없음 |
| `score` · `score_gap` 임계값 | 🔴 없음 |
| `dominant_ratio` 임계값 | 🔴 없음 |
| 그룹 일괄 확정 · 그룹핑 키 | 🔴 없음 |
| 분석 방법(BM25/RAG/FUSE) | 🔴 미선택 — 운영 기본은 `NoRuleClassifier` |
| 구매유형 업무규칙 | 🔴 고객 확인 (Q5-1~Q5-7) |
| DB-3 · Calculator 연결 | 🔴 미착수 |
