# PM 인수인계 — 여기서부터 읽으세요

## 문서 정보

| 항목 | 값 |
|---|---|
| 목적 | **새 PM 이 이 문서 하나로 프로젝트를 이어받을 수 있게 한다** |
| 최종 갱신 | 2026-08-17 |
| 작성 기준 | Git · 코드 · 테스트 **실측** (기억이나 추정이 아님) |
| 다음에 읽을 문서 | `DECISIONS.md` (결정의 단일 출처) → `UPLOAD_PIPELINE_DESIGN.md` (현재 작업) |

> 이 문서는 **현재 상태와 결정 대기 목록**만 담습니다. 결정의 정본은 여전히
> `DECISIONS.md` 이며, 새 결정은 그쪽에 기록합니다.

---

# 1. 30초 요약

**공공기관 우선구매 정책 달성률 자동 계산 시스템**입니다. 기관 담당자가 구매실적
엑셀을 올리면, 기업 인증 정보와 대조해 정책별 달성률을 계산하고 대시보드로
보여줍니다. 최종 산출물은 **Electron 데스크톱 앱**(Windows 설치형)입니다.

| 항목 | 상태 |
|---|---|
| 계산 엔진 · 대시보드 | ✅ 동작함 |
| 데스크톱 앱 구조 | ✅ 백엔드 생명주기 검증 완료 (실제 Electron 실행은 미검증) |
| 표준 엑셀 업로드 | 🟡 **검증까지 완료, 저장은 막혀 있음** |
| 실제 운영 데이터 | ❌ 아직 없음 (샘플만 분석) |
| **막고 있는 것** | 아래 §2 의 결정 3건 |

**품질 게이트는 전부 통과 상태입니다** — pytest 1053 · ruff · mypy strict ·
Electron 백엔드 검증 12/12.

---

# 2. 🔴 지금 당장 결정이 필요한 것 (3건)

## 2.1 [최우선] 이전 PM 지시서 2건이 서로 충돌합니다

| | 2026-08-15 지시서 §1 | 2026-08-16 지시서 §2-1 · §3 · §17 |
|---|---|---|
| `resolution_date` | **"최종 결정: 필드를 신설한다"** | **"임의 신설 금지, 승인 전"** |
| 창업기업 규칙 | (§1 결정의 논리적 귀결로 변경) | **"기존 규칙 유지"** |

**무슨 일이 있었나**

1. 08-15 지시서가 `resolution_date` 신설을 **"최종 결정"** 으로 명시 → 구현하고
   커밋(`84ebfee`)
2. 08-16 지시서가 **신설 금지**라고 지시. 다만 그 지시서는 기준 상태를
   `HEAD 057283f` 로 적고 있어 **08-15 결정 이전에 작성된 것으로 보임**

**현재 상태**: `84ebfee` 는 **그대로 두었습니다.** 되돌리는 것도 유지하는 것도
PM 결정이라 임의로 판단하지 않았습니다.

**결정해 주실 것**

| 선택지 | 필요한 작업 |
|---|---|
| **A. 유지** | 없음. 08-16 지시서의 §2-1·§3·§17 이 무효임을 확인만 |
| **B. 되돌림** | `84ebfee` revert (1커밋). 이후 08-16 지시서대로 진행 |

> 참고로 실무 관점에서는 **A(유지)를 권합니다.** 고객이 확정한 "산정 기준일 =
> 결의일자" 를 `payment_date`(지출완료일)에 섞지 않는 쪽이 의미가 정확하고,
> 되돌리면 08-15 결정을 다시 해야 합니다. 다만 이는 **제안일 뿐** 결정은
> PM 몫입니다.

## 2.2 업로드 저장이 막혀 있는 이유 — 지급일을 어떻게 채울 것인가

표준 양식 5개 컬럼에는 **지급일이 없는데**, 기존 적재 계층(`PurchaseImporter`)은
`payment_date` 를 **필수**로 요구합니다. 그래서 업로드는 **검증까지만** 하고
저장하지 않습니다.

| 안 | 내용 | 영향 |
|---|---|---|
| 1 | 표준 양식에 **지급일 컬럼 추가** | 양식 변경 → 고객 확인 필요 |
| 2 | `payment_date` 를 **선택 항목**(NULL 허용)으로 | 스키마 변경 · 일반 정책 판정 기준일 영향 |
| 3 | 업로드 경로에서만 **다른 날짜로 채움** | 확정되지 않은 규칙이 새로 생김 |

⛔ 임시로 아무 값이나 넣어 저장하지 않았습니다. 이 결정이 나면 Mapping 계층
(약 20줄)만 만들면 저장이 연결됩니다.

## 2.3 원격 push 승인

로컬 커밋 **9개가 푸시되지 않은 채** 쌓여 있습니다. 이전 PM 이 "명시적 push 승인
전까지 원격 금지" 를 계속 지시했기 때문입니다.

- 승인하시면 즉시 푸시합니다.
- 대상 브랜치를 알려 주세요: 현재 `claude/period-filter-import-batch` 그대로 /
  다른 브랜치

---

# 3. 현재 코드 상태 (실측)

| 항목 | 값 |
|---|---|
| 작업 브랜치 | `claude/period-filter-import-batch` |
| 로컬 HEAD | `703e57f` |
| 추적 원격 브랜치 | `origin/claude/period-filter-import-batch` = `12f11e4` |
| `origin/main` | `ca23f82` |
| **미푸시 커밋 (추적 브랜치 기준)** | **9개** |
| `origin/main` 과의 차이 | **26개** |
| 작업 트리 | clean |

> ⚠️ **"미푸시" 를 셀 때 기준을 반드시 밝힙니다.** 이 브랜치는 자기 원격
> 브랜치(`origin/claude/period-filter-import-batch`)에 이미 17개가 푸시되어
> 있고, 그 위에 **9개가 로컬에만** 있습니다. 반면 `origin/main` 에는 대시보드
> 까지만 병합되어 있어 **26개 차이**입니다. 두 숫자는 세는 대상이 다릅니다.
>
> 확인 명령:
> ```bash
> git rev-list --count @{u}..HEAD          # 9  ← 미푸시
> git rev-list --count origin/main..HEAD   # 26 ← main 과의 차이
> ```

## 3.1 미푸시 커밋 9개 (추적 브랜치 기준)

| 커밋 | 내용 |
|---|---|
| `b12fc85` | 구 스키마 DB 로 실행 시 500 나던 문제 방지 |
| `58752aa` | 창업기업 두 날짜 OR 판정 · 음수 상계 로직 |
| `bf7c1ff` | **Electron 데스크톱 전환 기반 구조** |
| `54fcdbe` | 표준 업로드 양식 정의 · 행 단위 검증 |
| `49f6b8f` | 문서 — 남은 구현 목록과 결정 지점 |
| `057283f` | 검증↔적재 이음매 고정 테스트 |
| `84ebfee` | **`resolution_date` 신설** (§2.1 충돌 대상) |
| `ff96059` | **표준 Excel 업로드 — 어댑터·양식·API·Electron 연결** |
| `703e57f` | 이 인수인계 문서 |

> 위 9개가 푸시되기 전에는 **Electron 도 업로드도 어느 원격 브랜치에도
> 없습니다.**

## 3.2 품질 게이트 (매번 이대로 유지)

```bash
.venv/bin/pytest -q          # 1053 passed
.venv/bin/ruff check .       # All checks passed
.venv/bin/mypy -p procurement && .venv/bin/mypy tests   # strict, 통과
node scripts/verify-backend.js                          # 12/12 통과
```

---

# 4. 확정된 업무규칙 — 다시 결정하지 마세요

고객 기관이 3차에 걸쳐 회신한 내용입니다. 전문은 `DECISIONS.md` §0 에 있습니다.

| # | 규칙 | 구현 |
|---|---|---|
| ① | **구매실적 기준일 = 결의일자** (연도 귀속) | ✅ `resolution_date` (§2.1 확인 대상) |
| ② | **구매금액 = VAT 포함 총액**, 원본의 `계` 컬럼. 공급가액 아님 | ✅ 양식·검증 |
| ③ | **음수 거래는 상계** — 기업명+사업자번호+절대금액이 같은 **이전** 양수와 1:1 | 🟡 로직만 (저장 제약 때문에 미연결) |
| ④ | **창업기업만 예외** — 결의일자 **OR** 계약일자 중 하나라도 인증 유효기간이면 인정 | ✅ 규칙 등록 |
| ⑤ | **정책 3종 제외** — 녹색제품 등은 범위 밖 | 🟡 GREEN seed 는 아직 그대로 (승인 대기) |
| ⑥ | **다중 정책 중복 인정** — 한 거래가 여러 정책에 잡혀도 분모는 1회만 | ✅ 검증 완료 |
| ⑦ | **구매유형 3건 확정** — 도서인쇄비·소모성물품구입비 → `GOODS`, 임차료 → `SERVICE` | 🟡 매핑만 (계산 미연결) |

**표준 업로드 양식 = 5개 컬럼 (확정)**

```
결의일자 | 계약일자 | 기업명 | 사업자등록번호 | 계
```

예산과목·구매유형·적요·대표자명·거래구분은 **확정되지 않아 양식에 넣지
않았습니다.** 칸이 있으면 사용자가 채우고, 그 값을 시스템이 해석하게 되어
확정되지 않은 규칙이 생기기 때문입니다.

---

# 5. 구현 현황

## 5.1 계층 구조 (이 순서를 지킵니다)

```
Electron (UI · 앱 생명주기)
    │  localhost HTTP  127.0.0.1
    ▼
FastAPI  →  ApiService  →  DataService  →  Calculator  →  Rule Engine
                                                              │
                                              Repository  →  SQLite
```

⛔ **업무 로직은 전부 Python 에 있습니다.** 계산·판정·검증을 JavaScript 로
재작성하지 않는 것이 이 프로젝트의 핵심 원칙입니다(§7 참조).

## 5.2 기능별 상태

| 영역 | 상태 | 위치 |
|---|---|---|
| 정책 달성률 계산 | ✅ | `calculators/` |
| 판정 규칙 엔진 | ✅ | `calculators/rules/` |
| 대시보드 화면·API | ✅ | `web/static/index.html` · `app.py` |
| 정책 목표율 관리 API | ✅ | `admin/` |
| 구매 적재 (행·배치) | ✅ | `importers/` |
| 기업 매칭 | ✅ | `matchers/` |
| 외부 인증 API 호출 | 🟡 코드만 — **실호출 미검증** | `collectors/` |
| **표준 양식 정의·검증** | ✅ | `uploads/format.py` · `validation.py` |
| **엑셀 읽기** | ✅ | `uploads/excel_adapter.py` |
| **양식 다운로드** | ✅ | `uploads/template.py` |
| **업로드 API** | ✅ 검증까지 | `POST /uploads/purchases/validate` |
| **업로드 → 저장** | ❌ **§2.2 결정 대기** | — |
| Electron 앱 | 🟡 백엔드 검증만 | `electron/` |
| 음수 상계 연결 | ❌ 저장 제약 해제 대기 | `core/offsetting.py` |

## 5.3 지금 실제로 되는 것

사용자 흐름 기준으로 **여기까지** 동작합니다.

```
표준 양식 다운로드  →  엑셀에 입력  →  파일 선택  →  업로드  →  전체 검증  →  결과 표시
                                                                          ↑ 여기까지 ✅
                                                              저장 ❌ (§2.2)
```

검증 결과는 이렇게 나옵니다.

```
3행 | 사업자등록번호 | 확인 | 사업자등록번호 체크섬이 일치하지 않습니다: 1234567890
4행 | 결의일자       | 오류 | 날짜 형식이 잘못되었습니다: '2026.13.45' (예: 2026-03-15)
4행 | 계             | 오류 | 숫자가 아닙니다: 'abc'
```

---

# 6. 결정·확인 대기 목록

## 6.1 PM 결정 필요

| # | 항목 | 막고 있는 것 |
|---|---|---|
| 1 | **§2.1 지시서 충돌** | 이후 모든 작업의 전제 |
| 2 | **§2.2 지급일 처리** | 업로드 저장 연결 |
| 3 | **§2.3 push 승인** | 원격 반영 |
| 4 | 대상 기간 입력 방식 | 저장 연결 (제안: 상단과 같은 연도 선택 → 1/1~12/31) |
| 5 | 음수 저장 제약 해제 | 상계 로직 연결 |
| 6 | `Purchase.purchase_type` 필드 신설 | 구매유형 계산 연결 |
| 7 | GREEN(녹색제품) 기존 DB 처리 | 정책 범위 정리 |
| 8 | 창업기업 API 운영 출처 | 인증 정보 수집 |
| 9 | 여성기업 3%/5% 목표율 구조 | 여성기업 계산 |
| 10 | Electron 배포 방식 | Windows 설치 파일 |
| 11 | 중복 업로드 판정 기준 | 재업로드 처리 |
| 12 | 최종 8개 정책 목록 | 정책 범위 확정 |
| 13 | 미확정 예산과목의 구매유형 | 구매유형 분류 |

## 6.2 고객 확인 필요

| # | 항목 | 막고 있는 것 |
|---|---|---|
| 1 | 계약일자 **원본 파일** | 창업기업 판정 |
| 2 | 계약일자 **연결 키** | 〃 |
| 3 | 계약일자 **매칭 방법** | 〃 |
| 4 | 외주용역비 등 나머지 예산과목의 구매유형 | 여성기업 계산 |
| 5 | 최종 8개 정책 목록 | 정책 범위 |
| 6 | 공사/용역/물품 구분 데이터 제공 가능 여부 | 여성기업 분모 |

---

# 7. 작업 원칙 — 왜 이렇게 하고 있는가

이전 PM 이 반복해서 지시한 원칙입니다. **이유가 있어 만들어진 것**이므로 배경을
함께 적어 둡니다. 바꾸실 수 있지만, 바꾼다는 사실을 알고 바꾸시는 편이 좋습니다.

| 원칙 | 배경 |
|---|---|
| **확정되지 않은 업무규칙을 추정해 구현하지 않는다** | 이 시스템은 **법정 실적 수치**를 산출합니다. 틀린 숫자가 나오면 기관이 잘못 보고하게 됩니다 |
| **업무 로직을 JavaScript 로 재작성하지 않는다** | 계산이 두 곳에 생기면 반드시 어긋납니다. Electron 은 UI·생명주기만 담당 |
| **기존 Importer / BatchImportService 를 재사용한다** | 적재 로직이 둘이 되면 데이터가 갈라집니다 |
| **테스트 기대값을 통과시키려고 바꾸지 않는다** | 업무규칙이 실제로 바뀐 경우에만 바꾸고, **사유를 docstring 에 남깁니다** |
| **`DECISIONS.md` 가 결정의 단일 출처** | 결정이 여러 문서에 흩어지면 어느 것이 최신인지 알 수 없게 됩니다 |
| **새 문서를 만들기 전에 기존 문서를 갱신할 수 있는지 본다** | 한때 문서가 39개까지 늘어 서로 모순됐고, 18개를 삭제해 정리한 이력이 있습니다 |
| **원격 push 는 승인 후** | 이전 PM 지시. 새 PM 이 해제하실 수 있습니다 |

### 구체적 금지 사항 (이유와 함께)

| 금지 | 이유 |
|---|---|
| 기표번호·결의번호·`LN_SQ`·`GRP_NB` 를 음수 판별키로 사용 | 샘플 음수 125건에서 **전부 비어 있음**이 실측됨 |
| 확정 3건 외 예산과목 자동 분류 (부분 문자열 매칭 포함) | "외주용역비" 등이 잘못 분류되면 달성률이 틀립니다 |
| `PURCHASE_PERIOD_DATE_FIELD` 기본값 지정 | 어느 날짜로 연도를 나눌지 확정 전 — 임의 기준일로 숫자를 만들지 않기 위함 |
| 실제 API 키를 코드·문서·`.env.example` 에 기록 | `.env` 로만 주입 |
| 최종 8개 정책을 임의 선정 / GREEN 삭제 | 고객 확인 대기 |

---

# 8. 실행 방법

```bash
# 최초 1회
python -m venv .venv && .venv/bin/pip install -e ".[dev]"

# DB 초기화 (멱등 — 기존 데이터를 지우지 않음)
.venv/bin/python -m procurement init

# 서버 실행 → http://127.0.0.1:8000/
.venv/bin/python -m procurement run

# 상태 점검
.venv/bin/python -m procurement health

# Electron (Electron 바이너리 필요)
npm install && npm start

# Electron 없이 백엔드 생명주기만 검증
node scripts/verify-backend.js
```

**대시보드에 숫자가 안 나올 때**는 대부분 두 가지입니다.

1. 정책 `target_rate` 가 `NULL` (의도된 기본값 — D-004). 목표율을 등록하면 계산됩니다
2. `PURCHASE_PERIOD_DATE_FIELD` 미설정 → 연도 조회가 503. 어느 날짜로 연도를
   나눌지 확정 전이라 **일부러** 기본값을 두지 않았습니다

---

# 9. 문서 지도

**먼저 읽을 3개**

| 순서 | 문서 | 내용 |
|---|---|---|
| 1 | **이 문서** | 현재 상태 · 결정 대기 |
| 2 | **`DECISIONS.md`** ⭐ | 결정의 단일 출처. §0 이 고객 확정 업무규칙 |
| 3 | `UPLOAD_PIPELINE_DESIGN.md` | 현재 작업(업로드)의 설계와 남은 결정 |

**필요할 때 보는 문서**

| 알고 싶은 것 | 문서 |
|---|---|
| 표준 양식 컬럼과 이유 | `STANDARD_UPLOAD_FORMAT.md` |
| Electron 구조·보안 설정 | `ELECTRON_ARCHITECTURE.md` |
| 고객에게 물어볼 것 | `CUSTOMER_DATA_QUESTIONS.md` |
| 데이터 확보 계획 · API 3종 | `DATA_ACQUISITION_PLAN.md` |
| 샘플 데이터 실측 결과 | `REAL_PURCHASE_DATA_ANALYSIS.md` |
| DB 스키마 | `DATABASE_DESIGN.md` |
| 정책 정의 | `POLICY_DEFINITION.md` |
| 전체 문서 목록 | `README.md` |

> ⚠️ `PROJECT_STATUS_AND_ROADMAP.md` 는 **2026-08-11 기준**이라 현재와 다릅니다.
> 현재 상태는 이 문서를 보세요.

---

# 10. 다음 작업 우선순위

| 순위 | 작업 | 선행 조건 | 예상 규모 |
|---|---|---|---|
| 1 | §2.1 충돌 정리 | **PM 판단** | 0 또는 revert 1건 |
| 2 | Mapping 계층 + 저장 연결 | §2.2 결정 | 작음 (기존 계층 재사용) |
| 3 | 대상 기간 선택 UI | 방식 승인 | 작음 |
| 4 | `npm install` → 실제 Electron 기동 확인 | 디스크·네트워크 | 중간 |
| 5 | Windows 동작 검증 | Windows 환경 | 중간 |
| 6 | 음수 상계 연결 | 저장 제약 해제 승인 | 중간 |
| 7 | 외부 인증 API 실호출 검증 | 키 주입 환경 | 중간 |

**2번이 끝나면 "엑셀 올려서 달성률 보기" 전체 흐름이 처음으로 완성됩니다.**
남은 것은 결정 하나(§2.2)이며 코드 규모는 크지 않습니다.

---

# 11. 새 PM 께 드리는 요청

가장 도움이 되는 순서로 정리했습니다.

1. **§2.1 충돌 판단** — A(유지) / B(되돌림)
2. **§2.2 지급일 처리 결정** — 1 / 2 / 3
3. **§2.3 push 승인 여부와 대상 브랜치**
4. 작업 지시 형식에 대한 선호 (이전 PM 은 §번호 지시서 + A~H 보고 형식을
   사용했습니다. 그대로 유지할지, 더 가볍게 갈지 알려 주시면 맞추겠습니다)

**진행 중 막히는 지점이 생기면 임의로 결정하지 않고 선택지와 함께 보고합니다.**
이 원칙은 새 지시가 없는 한 유지하겠습니다.
