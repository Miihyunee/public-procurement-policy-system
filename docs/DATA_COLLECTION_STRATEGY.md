# Data Collection Strategy

## 문서 정보

| 항목 | 내용 |
|------|------|
| Version | v1.0 |
| Status | Draft (조사 결과 기반 / 일부 항목 검증 필요) |
| Last Updated | 2026-08-06 |
| 관련 문서 | `DATA_SOURCES.md`(DS ID 체계), `DATA_MAPPING.md`, `POLICY_DEFINITION.md`, `DATA_DICTIONARY.md`, `PURCHASE_DATA_SPEC.md` |

---

# 1. 목적과 범위

## 1.1 목적

본 문서는 시스템이 정책별 우선구매 달성률을 계산하기 위해 **외부에서 확보해야 하는 데이터**와
그 **수집 전략**을 정의한다.

시스템의 계산 구조는 다음과 같다.

```
고객 구매(지출) 데이터  +  기업 인증정보(외부 수집)
        └────────── 사업자등록번호로 결합 ──────────┘
                            ↓
                  정책별 달성률 계산
```

즉 **외부 데이터 없이는 계산 자체가 성립하지 않으며**, 본 문서는 그 확보 방안을 다룬다.

## 1.2 범위

- 포함: 필요 데이터 목록, 제공기관·제공방식 조사, 수집 방식 제안, Collector 구조 설계
- 제외: 실제 Collector 구현, API 연동, DB 변경, Bootstrap 구현 (본 작업은 **설계 문서 작성만** 수행)

## 1.3 조사 신뢰도 표기 (중요)

본 문서의 조사 결과는 **공개된 웹 정보 기반**이며, 다음 한계가 있다.

- 공공데이터포털(data.go.kr) 상세 페이지는 자동 조회가 차단(HTTP 403)되어
  **응답 필드 단위까지는 직접 확인하지 못했다.**
- 따라서 각 항목에 **신뢰도**를 함께 표기한다.

| 표기 | 의미 |
|---|---|
| ✅ 확인 | 제공기관·제공방식이 복수 출처로 확인됨 |
| 🔶 추정 | 데이터셋 존재는 확인되나 세부 필드·조건은 미확인 |
| ❓ 미확인 | 존재 여부·조건 모두 추가 조사 필요 |

> **원칙**: 🔶·❓ 항목은 실제 활용신청 후 응답 스펙을 확인하기 전까지 구현에 착수하지 않는다.

---

# 2. 필요한 외부 데이터 목록

## 2.1 정책 인증 데이터 (기업 → 정책 자격)

| 구분 | 정책 | MVP 여부 | DS ID |
|---|---|---|---|
| A | 중소기업 | ✅ MVP | DS-001 |
| A | 여성기업 | ✅ MVP | DS-002 |
| A | 창업기업 | ✅ MVP | DS-003 |
| A | 장애인기업 | ✅ MVP | DS-004 |
| B | 녹색제품 | ✅ MVP(기업 기준) | DS-005 |
| C | 사회적기업 | 향후 | DS-007 (신규) |
| C | 자활기업 | 향후 | DS-008 (신규) |
| C | 중증장애인생산품 | 향후 | DS-009 (신규) |
| C | 국가유공자 자활용사촌 생산품 | 향후 | DS-010 (신규) |

- **구분 A**: 기업 단위 인증 + 유효기간 존재 → 현재 `Certification` 구조에 그대로 적재 가능
- **구분 B**: 제품 단위 인증 → 기업 단위 변환 필요 (MVP는 기업 기준으로 근사)
- **구분 C**: 시설·단체 지정 성격 → 구조 검토 필요 (5장 리스크 참조)

## 2.2 구매(지출) 데이터

| DS ID | 데이터 | 출처 | 비고 |
|---|---|---|---|
| DS-006 | 기관 구매실적 | **고객(공공기관) 제공** | 외부 수집 대상 아님. `PURCHASE_DATA_SPEC.md` 참조 |

---

# 3. 데이터별 조사표

> 열 정의 — **자동수집 가능 여부**는 "인증키 발급 후 프로그램이 주기적으로 가져올 수 있는가"를 뜻한다.

| 정책 | 제공기관 | 데이터명 | API 제공 여부 | API Key 필요 | 파일 다운로드 가능 | 사업자등록번호 제공 여부 | 자동수집 가능 여부 | 비고 |
|---|---|---|---|---|---|---|---|---|
| **중소기업** | 중소벤처기업부 / 중소기업기술정보진흥원(중소벤처24) | 중소기업확인서 등 증명서 정보 API | 🔶 제공(중소벤처24 Open API) | ✅ 필요 | ❌ | 🔶 제공 추정 | **⚠️ 조건부** | **인증키가 심사·승인제**. 신청서 제출 후 담당자 검토를 거쳐 발급. 민간 사업자 승인 가능 여부 **사전 확인 필수** |
| **여성기업** | 한국중소벤처기업유통원 (공공구매종합정보망 SMPP) | 공공구매종합정보망 인증서 정보 제공 서비스 | ✅ 제공 (data.go.kr, 15062581) | ✅ 필요 | ❌ | ✅ **요청 파라미터로 사용** | ✅ 가능 | 사업자번호 + 기준일자로 **여성기업확인서 상세 조회**. 본 프로젝트 요구와 가장 정합 |
| **장애인기업** | 한국중소벤처기업유통원 (SMPP) | 동일 API (위와 같은 서비스) | ✅ 제공 (15062581) | ✅ 필요 | ❌ | ✅ 요청 파라미터 | ✅ 가능 | 장애인기업확인서 상세 조회 가능. 여성기업과 **동일 Collector로 처리 가능** |
| **창업기업** | 중소벤처기업부 / 창업진흥원 (K-Startup) | 창업기업확인서 발급기업정보 조회서비스 | ✅ 제공 (data.go.kr, 15125362) | ✅ 필요 | 🔶 | 🔶 확인 필요 | ✅ 가능(추정) | 확인서 발급 기업 목록·제품정보 제공. **유효기간 필드 존재 여부 확인 필요** |
| **녹색제품** | 환경부 / 한국환경산업기술원 (녹색제품정보시스템) | 용도별·장소별·지역별 녹색제품 정보 | 🔶 파일→API 자동변환 | ✅ 필요(API 사용 시) | ✅ 가능 (CSV) | ❌ **미제공(업체명만)** | 🔶 제한적 | **제품 단위 데이터**(EL번호·모델명·인증상태). 사업자번호가 없어 **업체명 매칭 필요** → 정확도 리스크 |
| **사회적기업** | 고용노동부 / 한국사회적기업진흥원 | 인증 및 예비사회적기업 정보 / 사회적기업 목록 | 🔶 파일→API 자동변환 | ✅ 필요(API 사용 시) | ✅ 가능 | ❓ 확인 필요 | 🔶 제한적 | 기업명·인증번호·사회서비스분야 제공. **인증 유효기간 제공 여부 불명** |
| **자활기업** | 보건복지부 / 한국자활복지개발원 | 전국자활기업현황 (15091502) | ❌ 파일 중심 | ✅ 필요(API 사용 시) | ✅ 가능 | ❓ 확인 필요 | 🔶 제한적 | 시도·시군구·기업명·대표자·업종 제공. **유효기간 개념 없음** |
| **중증장애인생산품** | 보건복지부 (업무수행기관: 한국장애인개발원) | 중증장애인생산품 생산시설 지정현황 (3084473) | ❌ 파일 중심 | ✅ 필요(API 사용 시) | ✅ 가능 | ✅ **제공** | ✅ 가능(파일 기준) | **사업자번호 + 유효시작일 + 유효종료일 제공** → 현재 `Certification` 구조와 정합성 높음 |
| **국가유공자 자활용사촌** | 국가보훈부 | 자활용사촌 복지공장 생산품목 (15131425) | ❌ 파일 중심 | ✅ 필요(API 사용 시) | ✅ 가능 | ❓ 확인 필요 | ❌ 어려움 | 용사촌·복지공장 명칭/소재지/**조달청 물품목록번호** 제공. **부정기 갱신**, 구매 전 개별 확인 권고 |
| **기관 구매실적** | **고객(공공기관)** | 구매·지출 내역 | — | — | ✅ (Excel 업로드) | ✅ (요구 필수 컬럼) | ❌ 해당 없음 | 외부 수집 아님. 업로드 기능 및 규격 필요 |

### 3.1 조사 결론 요약

- **가장 중요한 발견**: SMPP 인증서 API(15062581) 하나로 **여성기업·장애인기업**을 사업자번호 기준으로
  조회할 수 있어, 프로젝트의 계산 모델과 정합성이 가장 높다.
- **가장 큰 제약**: **중소기업**은 5대 MVP 정책 중 비중이 가장 크지만, 인증키가 **심사·승인제**여서
  발급 가능 여부가 프로젝트 리스크의 핵심이다.
- **구조적 난제**: **녹색제품**은 사업자등록번호가 없어 시스템의 기본 결합키를 사용할 수 없다.

---

# 4. 수집 방식 제안

## 4.1 방식 선택 기준

| 방식 | 선택 기준 |
|---|---|
| **OpenAPI** | 사업자번호로 조회 가능하고 인증키 확보가 가능한 경우 (최우선) |
| **파일 다운로드** | 전수 데이터가 파일로 공개되며 갱신 주기가 길거나 부정기인 경우 |
| **웹 조회** | 공식 자동 수집 수단이 없고 건별 확인만 가능한 경우 (보조 수단) |
| **수기 입력** | 자동화가 불가하거나 건수가 적어 운영자가 직접 관리하는 편이 합리적인 경우 |

## 4.2 데이터별 권장 방식

| 정책 | 1차 권장 | 대체(Fallback) | 근거 |
|---|---|---|---|
| 중소기업 | **OpenAPI** (중소벤처24) | 고객이 확인서 사본 제출 → 수기/일괄 입력 | 인증키 심사 결과에 따라 대체안 필요 |
| 여성기업 | **OpenAPI** (SMPP) | — | 사업자번호 조회 지원, 대체 불필요 |
| 장애인기업 | **OpenAPI** (SMPP) | — | 여성기업과 동일 API |
| 창업기업 | **OpenAPI** (창업진흥원) | 파일 다운로드 | 유효기간 필드 확인 후 확정 |
| 녹색제품 | **파일 다운로드** | 업체명 기반 보정 + 수기 검수 | 사업자번호 부재로 자동 결합 한계 |
| 사회적기업 | **파일 다운로드** | 수기 입력 | 갱신 주기가 길고 전수 파일 제공 |
| 자활기업 | **파일 다운로드** | 수기 입력 | 동일 |
| 중증장애인생산품 | **파일 다운로드** | — | 사업자번호·유효기간 제공으로 적재 용이 |
| 국가유공자 자활용사촌 | **수기 입력** | 파일 다운로드(참고용) | 부정기 갱신 + 개별 확인 권고 |
| 기관 구매실적 | **파일 업로드(고객)** | — | 고객 보유 데이터 |

## 4.3 수집 주기 제안

| 유형 | 주기 | 비고 |
|---|---|---|
| 인증 조회형 API (SMPP 등) | **구매 데이터 업로드 시점에 조회** | 판정 기준일 기준으로 조회해야 정확 |
| 전수 파일형 | 월 1회 또는 분기 1회 | 원본 파일과 수집 이력 보관 |
| 부정기 데이터 | 운영자 수동 갱신 | 갱신 알림 절차 필요 |

> **설계 유의점**: 이 시스템은 "현재 인증 보유 여부"가 아니라
> **"판정 기준일에 인증이 유효했는가"** 로 실적을 판정한다(`POLICY_DEFINITION.md`).
> 따라서 수집 시 **유효기간(valid_from ~ valid_to)을 반드시 함께 확보**해야 하며,
> 기준일자 파라미터를 지원하는 API가 유리하다.

---

# 5. Collector 구조 제안

## 5.1 설계 원칙

1. **계층 원칙 유지** — Collector는 외부 데이터를 표준 형태로 변환하는 것까지만 담당하고,
   저장은 Repository에 위임한다. Calculator·Dashboard와 직접 결합하지 않는다.
2. **원본 보존** — 수집 원본(raw)을 그대로 보관하고, 변환(normalize) 결과를 별도로 만든다.
   외부 데이터 오류 발생 시 원인 추적이 가능해야 한다.
3. **정책별 격리** — 정책마다 제공기관·형식·조건이 모두 다르므로 정책 단위로 모듈을 나눈다.
4. **공통 규약** — 모든 Collector는 동일한 인터페이스를 따라 교체·추가가 쉬워야 한다.
   (`Rule Engine`에서 적용한 Strategy 방식과 동일한 사상)

## 5.2 권장 디렉터리 구조

```
src/procurement/collectors/
├── __init__.py
├── base.py                  # Collector 공통 인터페이스(추상) + 수집 결과 DTO
├── common/
│   ├── __init__.py
│   ├── http_client.py       # 공공데이터포털 공통 호출(인증키·재시도·타임아웃)
│   ├── file_loader.py       # CSV/XLSX 로더(인코딩·헤더 정규화)
│   ├── normalizer.py        # 사업자번호 정규화, 날짜 파싱, 기업명 정제
│   └── errors.py            # 수집 계층 예외 정의
├── smpp/                    # 공공구매종합정보망 — 여성기업·장애인기업
│   ├── __init__.py
│   └── certificate_collector.py
├── sme/                     # 중소기업 (중소벤처24)
│   └── __init__.py
├── startup/                 # 창업기업 (창업진흥원)
│   └── __init__.py
├── green/                   # 녹색제품 (한국환경산업기술원)
│   └── __init__.py
├── social/                  # 사회적기업 (한국사회적기업진흥원)
│   └── __init__.py
├── selfsupport/             # 자활기업 (한국자활복지개발원)
│   └── __init__.py
├── severe_disabled/         # 중증장애인생산품 (보건복지부)
│   └── __init__.py
└── veterans/                # 국가유공자 자활용사촌 (국가보훈부)
    └── __init__.py
```

### 구조 설명

- **`smpp/`를 별도로 둔 이유**: 여성기업·장애인기업이 **같은 API**로 제공되므로,
  정책별로 디렉터리를 쪼개면 동일 코드가 중복된다. **제공기관(데이터 소스) 기준**으로 묶는 것이 합리적이다.
  → 즉 디렉터리는 *정책*이 아니라 **데이터 소스** 단위로 나눈다.
- **`common/normalizer.py`**: 사업자등록번호 표기(`123-45-67890` vs `1234567890`)가 기관마다 달라
  **정규화가 필수**다. 이 규칙은 `DATA_MAPPING.md`의 표준 컬럼 규약과 연결된다.
- **MVP 우선순위**: `common/` → `smpp/` → `sme/` → `startup/` → `green/` 순으로 구현할 것을 제안한다.

## 5.3 공통 인터페이스(개념)

```python
# 개념 예시 — 구현 아님
class CollectedCertification:
    """수집 결과 표준 형태 (Certification 적재 전 단계)."""
    business_no: str          # 정규화된 사업자등록번호
    company_name: str
    policy_code: str
    valid_from: date | None
    valid_to: date | None
    certificate_number: str | None
    issuing_agency: str | None
    source: str               # DS ID 등 출처 식별자
    collected_at: datetime

class BaseCollector(Protocol):
    def collect(self, ...) -> list[CollectedCertification]: ...
```

- 각 Collector는 원본을 위 표준 형태로 변환해 반환하고, **저장은 상위 서비스가 Repository로 수행**한다.
- 이렇게 두면 Collector는 네트워크·파일 I/O만 책임지고 **테스트가 쉬워진다**.

---

# 6. 리스크 (Risk)

> 아래 3건은 **현재 코드 구조와 외부 데이터의 실제 구조가 어긋나는 지점**으로,
> 수집 구현 전에 결정이 필요하다.

## R-1. 녹색제품 — 사업자등록번호 부재 🔴 높음

- 녹색제품 데이터는 **제품 단위**(EL번호·모델명)이며 **업체명만 제공**된다.
- 그러나 본 시스템은 **사업자등록번호를 모든 결합의 기준 키**로 사용한다(`DATA_MAPPING.md`).
- **영향**: 녹색제품 정책만 자동 결합이 불가능하다. 업체명 매칭은 동명이인·법인격 표기 차이
  (`(주)`, `주식회사`)로 오매칭 위험이 있다.
- **대응 후보**: ① 업체명 정규화 매칭 + 검수 절차, ② 고객이 녹색제품 구매분을 별도 표기,
  ③ MVP에서 녹색제품 자동수집 제외.

## R-2. 중소기업 API — 인증키 승인제 🔴 높음

- 중소벤처24 Open API 인증키는 **신청 후 심사**를 거쳐 발급되며, 자동 승인이 아니다.
- **영향**: 5대 정책 중 가장 비중이 큰 중소기업 데이터를 확보하지 못할 수 있다.
- **대응 후보**: ① 조기 신청(리드타임 확보), ② 고객이 확인서를 제출하는 수기 경로 병행 설계.

## R-3. 인증 유효기간이 없는 데이터 🟡 중간

- `Certification` 모델은 `valid_from`·`valid_to`가 **필수(NOT NULL)** 이다.
- 그러나 **자활기업·사회적기업·자활용사촌** 데이터는 유효기간 개념이 없거나 제공되지 않는다.
- **영향**: 해당 정책은 현재 구조에 그대로 적재할 수 없다.
- **대응 후보**: ① 유효기간 nullable 허용(스키마 변경), ② 수집일 기준 기간을 부여하는 규칙 수립,
  ③ Rule Engine에서 "기간 무관" 규칙으로 처리(`ISSUE17-rule-engine-design.md`의 확장 방향과 연결).

## R-4. 대표자명 필수 제약 🟡 중간

- `Company.representative_name`이 **필수**이나, 인증 조회 API가 대표자명을 제공하지 않을 수 있다.
- **영향**: 수집 데이터만으로 `Company` 생성이 불가능할 수 있다.
- **대응 후보**: nullable 허용 또는 구매 데이터의 업체명으로 대체 후 보완.

## R-5. 개인정보·이용약관 🟡 중간

- 기업 인증정보는 사업자 정보이나, **API 이용약관상 재배포·저장 제한**이 있을 수 있다.
- **대응**: 각 API 활용신청 시 이용범위 확인 및 문서화 필요.

## R-6. 조사 미검증 항목 🟡 중간

- 3장 표의 🔶·❓ 항목은 **응답 필드 수준 검증이 되지 않았다**(포털 자동조회 차단).
- **대응**: 활용신청 후 실제 응답 샘플을 확보해 본 문서를 v1.1로 갱신한다.

---

# 7. 다음 단계 제안

| 순서 | 작업 | 비고 |
|---|---|---|
| 1 | 공공데이터포털 계정 생성 및 **활용신청** (SMPP 15062581, 창업진흥원 15125362) | 승인 대기 시간 존재 |
| 2 | **중소벤처24 인증키 신청** | 심사제 — 가장 먼저 착수 권장 |
| 3 | 응답 샘플 확보 후 본 문서 v1.1 갱신 + `DATA_SOURCES.md` TODO 채우기 | 필드 검증 |
| 4 | R-1·R-3·R-4 구조 결정 (PM) | 스키마 영향 있음 |
| 5 | `common/` + `smpp/` Collector 구현 착수 | 가장 정합성 높은 소스부터 |

> **Bootstrap(Issue #23)과의 관계**: Bootstrap은 초기 정책(Policy) 데이터를 생성한다.
> 본 문서에서 정책 목록이 9종으로 확장되었으므로, **Bootstrap의 seed 대상 범위를
> MVP 5종으로 한정할지 여부**를 확정한 뒤 구현하는 것이 안전하다.
