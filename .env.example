# ==============================================================================
# Public Procurement Policy System — 환경변수 예시 파일
#
# 사용 방법:
#   cp .env.example .env
#   .env 파일을 열어 실제 값으로 수정합니다.
#
# 주의:
#   .env 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다.
#   민감한 정보(API 키, 비밀번호 등)는 반드시 .env에서 관리하세요.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application
# ------------------------------------------------------------------------------
APP_NAME=Public Procurement Policy System
APP_VERSION=0.1.0

# 실행 환경: development | staging | production
ENVIRONMENT=development

# 디버그 모드: True | False  (운영 환경에서는 반드시 False)
DEBUG=True

# ------------------------------------------------------------------------------
# Admin API — 설정 변경(쓰기) API 인증
#
#   PUT /policies/{policy_code}/target-rate 는 아래 토큰이 설정되어 있을 때만
#   활성화됩니다. 미설정 시 쓰기 API 는 503 으로 응답하며, 조회 API
#   (GET /policies, GET /dashboard/summary)는 영향을 받지 않습니다.
#
#   요청 예: Authorization: Bearer <토큰>
#
#   ⚠️ 실제 토큰 값은 .env 에만 두고 코드·문서·저장소에 기록하지 마세요.
#      아래는 예시 자리표시자이며 실제 값이 아닙니다.
# ------------------------------------------------------------------------------
# ADMIN_API_TOKEN=change-me-to-a-random-secret

# ------------------------------------------------------------------------------
# Dashboard — 데이터 모드
#
#   demo        : 화면에 'DEMO / SAMPLE DATA' 배지를 표시합니다 (기본값)
#   operational : 실제 운영 데이터가 적재된 뒤에만 사용합니다
# ------------------------------------------------------------------------------
DATA_MODE=demo

# ------------------------------------------------------------------------------
# 기간(연도) 판정 기준일
#
#   ⛔ 기본값이 없습니다. 비워 두세요.
#
#   어느 날짜로 연도를 나눌지는 아직 확정되지 않았습니다
#   (D-24 — 고객 확인 항목 W-1 에 종속).
#   비어 있으면 연도별 조회가 503 으로 거부되고, 전체 데이터 기준으로만
#   계산됩니다. 확정 전에 임의의 값을 넣으면 근거 없는 숫자가 나옵니다.
#
#   확정 후 payment_date 또는 contract_date 중 하나를 지정합니다.
# ------------------------------------------------------------------------------
# PURCHASE_PERIOD_DATE_FIELD=

# ------------------------------------------------------------------------------
# 외부 인증 API 키
#
#   ⚠️ 이 파일에는 **변수명과 설명만** 둡니다. 실제 키 값을 여기에 적지 마세요.
#      실제 값은 .env 에만 두고, 코드·테스트·문서·저장소·메신저에 남기지 않습니다.
#
#   ⚠️ 공공데이터포털은 Encoding / Decoding 두 가지 키를 줍니다.
#      **Decoding 키**를 넣으세요. 코드가 URL Encode 를 수행하므로,
#      Encoding 키를 넣으면 이중 인코딩되어 인증 실패(결과코드 30)가 납니다.
#
#   ⚠️ 결과코드 32(등록되지 않은 IP·도메인)는 키가 맞아도 발생합니다.
#      호출하는 서버의 IP 를 포털 활용신청 내역에 등록해야 합니다.
# ------------------------------------------------------------------------------

# 공공구매종합정보망(SMPP) — 여성기업 · 장애인기업 · 창업기업(SMPP) 조회에 사용
# SMPP_API_KEY=

# 창업진흥원 — 창업기업확인서 발급기업정보 조회에 사용
# STARTUP_API_KEY=

# 응답 대기 시간(초). 미지정 시 10.0
# EXTERNAL_API_TIMEOUT_SECONDS=

# 최대 시도 횟수(최초 1회 포함). 미지정 시 2.
#   재시도는 timeout · 네트워크 오류 · HTTP 5xx 에만 적용됩니다.
#   인증 실패 · 잘못된 요청 · 일일 한도 초과는 재시도하지 않습니다.
# EXTERNAL_API_MAX_ATTEMPTS=

# --- 아직 코드가 읽지 않는 키 (해당 API 미연동) ---------------------------------
# SME_API_KEY=               # 중소벤처24 — 미신청 상태
# SMPP_COOPERATION_API_KEY=  # 상생협력 확인서 — 현재 정책 범위 밖
# SMPP_PERFORMANCE_API_KEY=  # 성능인증 확인서 — 현재 정책 범위 밖

# ------------------------------------------------------------------------------
# Path — 값을 지정하지 않으면 프로젝트 루트 아래 기본 경로를 사용합니다.
# ------------------------------------------------------------------------------

# 구매실적 Excel, 수집 데이터 등 데이터 파일 저장 경로
# DATA_PATH=/absolute/path/to/data

# SQLite 데이터베이스 파일 저장 디렉터리
# DATABASE_PATH=/absolute/path/to/database

# SQLite 데이터베이스 파일명 (기본값: procurement.db)
# DATABASE_FILENAME=procurement.db

# 로그 파일 저장 경로
# LOG_PATH=/absolute/path/to/logs